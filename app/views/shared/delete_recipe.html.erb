<% if can (:detroy, recipe) %>
    <%= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' } %>
     <%
=begin%>
 <%= for_with (model: recipe, method: :delete, data: {confirm: 'Are you sure?'}, url: recipe_path(recipe), class: 'delete-recipe delete-buttons') do |form| %>
    <%= form.submit 'Remove' %> 
<%
=end%>
    <% end %>
<% end %>